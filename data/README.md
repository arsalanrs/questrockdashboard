# Shape export data

- **shape-export-sample.json** – Raw response from Shape’s bulk export API (one page). Created when you run:

  ```bash
  node scripts/fetch-shape-to-file.js
  ```

  Use it to see how Shape stores data (field names, record keys, and sample values) before syncing to Supabase.
